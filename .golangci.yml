linters:
  enable:
    - contextcheck
    - gci
    - gocritic
    - gofumpt
    - gosimple
    - ineffassign
    - misspell
    - nonamedreturns
    - staticcheck
  disable:
    - errcheck

linters-settings:
  gci:
    sections:
      - standard
      - default
      - prefix(github.com/containernetworking)

run:
  skip-dirs:
    - vendor
